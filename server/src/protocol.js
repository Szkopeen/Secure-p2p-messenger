// Legacy relay protocol validators were removed with the shared-token relay.
// The active protocol is implemented in v2Store.js and authenticates WebSocket
// connections with short-lived, single-use tickets.