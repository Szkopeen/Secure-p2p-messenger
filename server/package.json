{
  "name": "secure-p2p-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Prywatny serwer WebSocket do signalingu i przekazywania zaszyfrowanych pakietow E2EE.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node scripts/check-conflicts.js && node --check src/config.js && node --check src/sqliteStore.js && node --check src/v2Store.js && node --check src/server.js",
    "test": "node --test test/*.test.js",
    "admin:users": "node scripts/admin-users.js",
    "generate-update-key": "node scripts/generate-update-signing-key.js",
    "publish-update": "node scripts/publish-update.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ws": "^8.18.0"
  }
}
